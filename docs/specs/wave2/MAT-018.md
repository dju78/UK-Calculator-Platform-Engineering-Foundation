# MAT-018 - Long Division Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-018` |
| Name | Long Division Calculator |
| Category | Maths & Algebra |
| Subcategory | Arithmetic |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/long-division-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Divide two whole numbers and show the working, step by step.

## Scope

Positive whole numbers.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `dividend` | Dividend | number | 1234 | The number being divided. |
| `divisor` | Divisor | number | 7 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Division by zero is refused.
- Negative inputs are refused with an explanation, because the long-division layout is defined for magnitudes.

## Formula and logic

Each digit is brought down in turn, the quotient digit found, the product subtracted and the remainder carried forward - the method taught in schools, recorded as a table of steps.

## Outputs

- `quotient`
- `remainder`
- `decimal_result`
- `is_exact`
- `mixed_number`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Exact division** - {"dividend":144,"divisor":12}
- **With a remainder** - {"dividend":1234,"divisor":7}
- **Divisor larger than dividend** - {"dividend":5,"divisor":20}
- **Dividing by one** - {"dividend":987,"divisor":1}
- **Large dividend** - {"dividend":100000,"divisor":37}
- **Divisor equals dividend** - {"dividend":42,"divisor":42}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Integer division with a remainder. The decimal result is shown alongside but is not expanded step by step.

## Methodology

The oracle finds the quotient and remainder by REPEATED SUBTRACTION, which is the definition of integer division rather than an application of the floor function.

## Related calculators

- MAT-016 Greatest Common Factor
- MAT-001 Basic Calculator

## SEO metadata

- Title: `Long Division Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/long-division-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
