# STA-005 - Z-Score Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-005` |
| Name | Z-Score Calculator |
| Category | Statistics & Data |
| Subcategory | Inference |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/z-score-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Express a value as a number of standard deviations from the mean, with the percentile that implies.

## Scope

One value against a known mean and standard deviation.

## Assumptions

- The percentile assumes the data follow a normal distribution.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `value` | Value | number | 115 | - |
| `mean` | Mean | number | 100 | - |
| `standard_deviation` | Standard deviation | number | 15 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- The standard deviation must be greater than zero: with no spread, no value is unusual.

## Formula and logic

z is the value less the mean, divided by the standard deviation. The percentile is the normal cumulative distribution function at z.

## Outputs

- `z_score`
- `percentile`
- `probability_below`
- `probability_above`
- `two_tailed_p_value`
- `standard_deviations_from_mean`
- `is_outlier_at_two_sd`
- `is_outlier_at_three_sd`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **One standard deviation above the mean** - {"value":115,"mean":100,"standard_deviation":15}
- **Below the mean** - {"value":85,"mean":100,"standard_deviation":15}
- **Exactly at the mean** - {"value":100,"mean":100,"standard_deviation":15}
- **Beyond three standard deviations** - {"value":150,"mean":100,"standard_deviation":15}
- **Exam mark in a tight distribution** - {"value":72,"mean":65,"standard_deviation":4}
- **Two standard deviations below** - {"value":70,"mean":100,"standard_deviation":15}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

For skewed data such as incomes or waiting times, a z-score still measures distance from the mean but the percentile it implies will be wrong. The calculator says so rather than presenting the percentile as a fact.

## Methodology

Benchmarks come from an independent oracle that shares NO special-function code with the engine. Where the engine evaluates the t, chi-square and F distributions through the regularised incomplete beta and incomplete gamma functions, the oracle integrates each density numerically by Simpson's rule and obtains the normalising constant by integrating the unnormalised density over its whole support. No gamma function is used on that side at all, and infinite ranges are handled by substitution rather than truncation, so no tail is silently discarded.

## Related calculators

- STA-004 Variance
- STA-007 P-Value

## SEO metadata

- Title: `Z-Score Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/z-score-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
