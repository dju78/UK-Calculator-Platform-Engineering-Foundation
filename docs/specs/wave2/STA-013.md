# STA-013 - Correlation Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-013` |
| Name | Correlation Calculator |
| Category | Statistics & Data |
| Subcategory | Regression |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/correlation-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Measure the association between two variables, both as a straight line and as a rank order.

## Scope

Paired X and Y values.

## Assumptions

- The pairs are independent observations.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `x_values` | X values | text | "1 | - |
| `y_values` | Y values | text | "2 | The same number of values as X. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- X and Y must have the same length and at least three pairs.
- A variable that never changes is refused, because there is no relationship to measure.
- Inputs are read by the shared dataset parser, which accepts commas, spaces, semicolons, new lines and JSON array notation, and REFUSES any token it cannot read rather than dropping it. Silently discarding an unreadable value would hand the user a confident answer to the wrong data.

## Formula and logic

Pearson's r is the covariance over the product of the standard deviations. Significance is a t test on r with n-2 degrees of freedom. Spearman's rho is Pearson's r applied to the RANKS, with tied values sharing the average of the ranks they span.

## Outputs

- `n`
- `pearson_r`
- `r_squared`
- `spearman_rho`
- `covariance_sample`
- `t_statistic`
- `degrees_of_freedom`
- `p_value`
- `significant_at_5_percent`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Strong positive relationship** - {"x_values":"[1,2,3,4,5,6,7,8]","y_values":"[2,4,5,4,5,7,8,9]"}
- **Perfect straight line** - {"x_values":"[1,2,3,4,5]","y_values":"[3,5,7,9,11]"}
- **Strong negative relationship** - {"x_values":"[1,2,3,4,5,6]","y_values":"[12,10,9,6,4,3]"}
- **Almost no relationship** - {"x_values":"[1,2,3,4,5,6,7,8]","y_values":"[5,3,8,2,7,4,6,5]"}
- **Monotonic but curved, where Spearman beats Pearson** - {"x_values":"[1,2,3,4,5,6,7]","y_values":"[1,4,9,16,25,36,49]"}
- **With tied values** - {"x_values":"[1,2,2,3,4,5]","y_values":"[2,3,3,5,6,8]"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Correlation is not causation, and Pearson's r measures only STRAIGHT-LINE association: a perfect curved relationship can return an r well below 1. Spearman is reported alongside because it captures any consistently increasing or decreasing relationship, and a benchmark on y = x squared has a Spearman of exactly 1 while Pearson is lower. A statistical result on the data entered. Statistical significance is not the same as practical importance, and no test can tell you whether the data were collected in a way that supports the conclusion you want to draw.

## Methodology

Benchmarks come from an independent oracle that shares NO special-function code with the engine. Where the engine evaluates the t, chi-square and F distributions through the regularised incomplete beta and incomplete gamma functions, the oracle integrates each density numerically by Simpson's rule and obtains the normalising constant by integrating the unnormalised density over its whole support. No gamma function is used on that side at all, and infinite ranges are handled by substitution rather than truncation, so no tail is silently discarded. The rank correlation is computed independently with its own tie handling.

## Related calculators

- STA-014 Linear Regression
- STA-015 R-Squared

## SEO metadata

- Title: `Correlation Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/correlation-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
