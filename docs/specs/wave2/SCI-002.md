# SCI-002 - Voltage Drop Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `SCI-002` |
| Name | Voltage Drop Calculator |
| Category | Science & Engineering |
| Subcategory | Electrical |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/voltage-drop-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

Check the voltage lost along a cable run against the limits BS 7671 sets.

## Scope

A design current, a run length, a conductor size and material, a supply system and an operating temperature.

## Assumptions

- A resistive calculation, which is what the standard's own method reduces to for smaller cables.
- A balanced load on a three-phase circuit.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `current` | Design current (A) | number | 32 | - |
| `length_m` | Cable run (m) | number | 25 | The one-way length; the return conductor is allowed for in the calculation. |
| `csa_mm2` | Conductor size (mm²) | number | 6 | - |
| `conductor` | Conductor material | select | "copper" | - |
| `system` | Supply system | select | "single_phase" | - |
| `nominal_voltage` | Nominal voltage (V) | number | 230 | - |
| `circuit_use` | Circuit type | select | "other" | - |
| `operating_temperature` | Conductor operating temperature (°C) | number | 70 | 70 °C for standard thermoplastic insulation. Calculating at 20 °C flatters the design by about a fifth. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Currents, lengths and conductor sizes beyond any real installation are refused rather than answered.
- The operating temperature must be between -50 and 250 degrees Celsius.

## Formula and logic

The drop is the system factor times the one-way conductor resistance times the current, where the resistance uses a resistivity corrected to the operating temperature.

## Outputs

- `voltage_drop`
- `voltage_drop_pct`
- `permitted_pct`
- `permitted_volts`
- `within_limit`
- `maximum_length_within_limit`
- `voltage_at_load`
- `conductor_resistance_ohms`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A 32 A ring in 6 mm squared copper at operating temperature** - {"current":32,"length_m":25,"csa_mm2":6,"conductor":"copper","system":"single_phase","operating_temperature":70,"nominal_voltage":230,"circuit_use":"other"}
- **A lighting circuit against the tighter 3 per cent limit** - {"current":6,"length_m":30,"csa_mm2":1.5,"conductor":"copper","system":"single_phase","operating_temperature":70,"nominal_voltage":230,"circuit_use":"lighting"}
- **A long submain that fails the limit** - {"current":40,"length_m":80,"csa_mm2":10,"conductor":"copper","system":"single_phase","operating_temperature":70,"nominal_voltage":230,"circuit_use":"other"}
- **Three phase, where the factor is root three rather than two** - {"current":63,"length_m":50,"csa_mm2":16,"conductor":"copper","system":"three_phase","operating_temperature":70,"nominal_voltage":400,"circuit_use":"other"}
- **Aluminium, which is about two thirds as conductive** - {"current":100,"length_m":40,"csa_mm2":35,"conductor":"aluminium","system":"three_phase","operating_temperature":70,"nominal_voltage":400,"circuit_use":"other"}
- **The same cable calculated at ambient rather than operating temperature** - {"current":32,"length_m":25,"csa_mm2":6,"conductor":"copper","system":"single_phase","operating_temperature":20,"nominal_voltage":230,"circuit_use":"other"}
- **Direct current, where the factor is two** - {"current":20,"length_m":15,"csa_mm2":4,"conductor":"copper","system":"dc","operating_temperature":30,"nominal_voltage":24,"circuit_use":"other"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

THE SYSTEM FACTOR IS 2 FOR DC AND SINGLE-PHASE, ROOT THREE FOR THREE-PHASE. The current goes out along one conductor and back along another, which is where the 2 comes from; a balanced three-phase circuit measured line to line uses the square root of three instead. Using 2 for three phase overstates the drop by about fifteen per cent and is the commonest error in a hand calculation. RESISTIVITY RISES WITH TEMPERATURE, by about a fifth between 20 and 70 degrees for copper, so calculating at ambient flatters every design; the operating temperature is an explicit input for that reason. ABOVE ABOUT 25 SQUARE MILLIMETRES the cable's inductive reactance becomes a material part of the drop and this resistive figure reads low; the calculator says so rather than being silently optimistic, and points to the tabulated millivolt per amp per metre values. This is a CHECK, NOT A COMPLIANCE CERTIFICATE. A real design must consult the standard and must also satisfy current-carrying capacity, earth fault loop impedance and disconnection times, none of which this addresses.

## Methodology

The oracle computes the conductor resistance in ohm millimetres squared per metre, a different unit chain from the engine's ohm metres and square metres, and cross-checks that against a thousand-segment summation along the run before emitting each case. Benchmarks include the same cable at 20 and at 70 degrees so the temperature correction is pinned, and a failing case whose reported maximum length is then fed back in and asserted to pass.

## Related calculators

- SCI-001 Ohm's Law

## SEO metadata

- Title: `Voltage Drop Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/voltage-drop-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
