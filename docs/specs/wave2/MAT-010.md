# MAT-010 - Log Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-010` |
| Name | Log Calculator |
| Category | Maths & Algebra |
| Subcategory | Algebra |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/log-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Take a logarithm to any base, with the three common bases alongside.

## Scope

One value and one base.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `value` | Value | number | 1000 | - |
| `base` | Base | number | 10 | 10 for the common log, 2 for binary. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- The value must be above zero; the base must be positive and not equal to 1.

## Formula and logic

log_b(x) = ln(x) / ln(b). The check figure raises the base back to the result to show the round trip.

## Outputs

- `logarithm`
- `natural_log`
- `log_base_10`
- `log_base_2`
- `check`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Common logarithm** - {"value":1000,"base":10}
- **Binary logarithm** - {"value":1024,"base":2}
- **Natural logarithm** - {"value":2.718281828459045,"base":2.718281828459045}
- **Non-integer result** - {"value":50,"base":10}
- **Base larger than the value** - {"value":5,"base":10}
- **Fractional base** - {"value":8,"base":0.5}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Real logarithms only: the logarithm of zero or a negative number is not defined over the reals and is refused.

## Methodology

Independently derived, including a fractional base, where the logarithm is negative for values above 1.

## Related calculators

- MAT-008 Exponent
- MAT-011 Scientific Notation

## SEO metadata

- Title: `Log Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/log-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
