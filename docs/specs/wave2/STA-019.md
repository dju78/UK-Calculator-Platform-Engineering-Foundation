# STA-019 - A/B Test Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-019` |
| Name | A/B Test Calculator |
| Category | Statistics & Data |
| Subcategory | Experimentation |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/a-b-test-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Decide whether a conversion experiment has actually shown anything.

## Scope

Two groups of visitors and conversions, at a chosen confidence level.

## Assumptions

- Visitors were assigned at random and each is counted once.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `control_visitors` | Control visitors | number | 10000 | - |
| `control_conversions` | Control conversions | number | 500 | - |
| `variant_visitors` | Variant visitors | number | 10000 | - |
| `variant_conversions` | Variant conversions | number | 600 | - |
| `confidence` | Confidence level | number | 0.95 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Visitor counts must be whole numbers of 1 or more.
- Conversions cannot exceed visitors.

## Formula and logic

The test statistic uses the POOLED standard error, which assumes the null hypothesis. The confidence interval uses the UNPOOLED standard error, because an interval must not assume the very thing it is estimating. Using one for both is a common and quiet error.

## Outputs

- `control_rate`
- `variant_rate`
- `absolute_difference`
- `relative_lift`
- `z_statistic`
- `p_value`
- `significant_at_5_percent`
- `confidence_interval_lower`
- `confidence_interval_upper`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Clear winner** - {"control_visitors":10000,"control_conversions":500,"variant_visitors":10000,"variant_conversions":600,"confidence":0.95}
- **Too close to call** - {"control_visitors":1000,"control_conversions":50,"variant_visitors":1000,"variant_conversions":55,"confidence":0.95}
- **Small sample, large apparent lift** - {"control_visitors":200,"control_conversions":20,"variant_visitors":200,"variant_conversions":30,"confidence":0.95}
- **Variant performs worse** - {"control_visitors":5000,"control_conversions":400,"variant_visitors":5000,"variant_conversions":340,"confidence":0.95}
- **Very large sample, tiny difference** - {"control_visitors":200000,"control_conversions":10000,"variant_visitors":200000,"variant_conversions":10300,"confidence":0.95}
- **Unequal traffic split** - {"control_visitors":8000,"control_conversions":400,"variant_visitors":2000,"variant_conversions":130,"confidence":0.95}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

'Not significant' and 'no difference' are different statements, and a wide interval says which one you have - so the interval, whether it contains zero, and the sample that would be needed are all reported. Stopping a test as soon as it turns significant inflates false positives badly: decide the sample size first and run to it. A statistical result on the data entered. Statistical significance is not the same as practical importance, and no test can tell you whether the data were collected in a way that supports the conclusion you want to draw.

## Methodology

Benchmarks come from an independent oracle that shares NO special-function code with the engine. Where the engine evaluates the t, chi-square and F distributions through the regularised incomplete beta and incomplete gamma functions, the oracle integrates each density numerically by Simpson's rule and obtains the normalising constant by integrating the unnormalised density over its whole support. No gamma function is used on that side at all, and infinite ranges are handled by substitution rather than truncation, so no tail is silently discarded. Benchmarks include a very large sample with a tiny difference and a small sample with a large apparent one, which behave in opposite ways.

## Related calculators

- STA-010 Statistical Power
- STA-009 Margin of Error

## SEO metadata

- Title: `A/B Test Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/a-b-test-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
