# HOM-006 - Stair Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `HOM-006` |
| Name | Stair Calculator |
| Category | Home & Construction |
| Subcategory | Construction |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/stair-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Design a straight flight of private stairs and check it against Approved Document K.

## Scope

A floor-to-floor rise, a preferred rise per step and a going.

## Assumptions

- A straight private flight serving a single dwelling.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `total_rise_mm` | Total rise, floor to floor (mm) | number | 2600 | The finished floor level below to the finished floor level above. |
| `preferred_rise_mm` | Preferred rise per step (mm) | number | 190 | The actual rise will differ, because the number of steps has to be a whole number. |
| `going_mm` | Going, the depth of each tread (mm) | number | 230 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- A total rise below 100 mm or above 10 m is refused, with a reminder that the figure is in millimetres.
- A preferred rise outside 50 to 400 mm and a going outside 50 to 600 mm are refused.

## Formula and logic

The riser count is the whole number nearest the total rise divided by the preferred rise; the actual rise follows from that count. Pitch is the arctangent of rise over going, and the stride figure is twice the rise plus the going.

## Outputs

- `number_of_risers`
- `number_of_treads`
- `actual_rise_mm`
- `going_mm`
- `total_run_mm`
- `pitch_degrees`
- `two_rise_plus_going_mm`
- `stringer_length_mm`
- `all_checks_pass`
- `failed_checks`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A typical domestic flight that complies** - {"total_rise_mm":2600,"preferred_rise_mm":190,"going_mm":230}
- **A steep flight that breaks three rules at once** - {"total_rise_mm":2600,"preferred_rise_mm":230,"going_mm":200}
- **A low rise to a raised floor** - {"total_rise_mm":900,"preferred_rise_mm":180,"going_mm":250}
- **A tall Victorian storey height** - {"total_rise_mm":3200,"preferred_rise_mm":195,"going_mm":225}
- **A shallow flight that fails the stride rule from below** - {"total_rise_mm":2400,"preferred_rise_mm":150,"going_mm":220}
- **A generous going that fails the stride rule from above** - {"total_rise_mm":2700,"preferred_rise_mm":200,"going_mm":320}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

THE NUMBER OF RISERS MUST BE A WHOLE NUMBER, so the actual rise is almost never the one asked for, and every check runs against the actual figure because that is what gets built. A flight also has ONE MORE RISER THAN IT HAS TREADS, since the top surface is the landing; an off-by-one there shortens the calculated run by a full going. THE 2R+G RULE IS NOT ARBITRARY: it encodes the length of a human stride on a slope, which is why a stair outside 550 to 700 mm feels wrong to walk even when the rise and going are each individually legal. It can fail from either direction, and both are benchmarked. THIS IS A CHECK, NOT A COMPLIANCE CERTIFICATE. The Approved Documents are guidance on one way of meeting the Building Regulations rather than the Regulations themselves; they apply to England, and Scotland, Wales and Northern Ireland publish their own. The figures used are for a PRIVATE stair serving a single dwelling, and common, institutional and assembly stairs are tighter, so applying these to them would wrongly pass a non-compliant design. The calculator also says nothing about headroom, handrails, guarding or landings, all of which the same document governs, and a real design is signed off by building control.

## Methodology

The oracle finds the riser count by SEARCHING for the whole number giving a rise closest to the preferred one rather than by rounding a quotient, and derives the pitch from an arcsine on the step hypotenuse rather than an arctangent on rise over going. Cases fail the stride rule from both directions, so a comparison written the wrong way round would pass one and fail the other. Unit tests assert that the risers multiply back to exactly the total rise, that there is always one more riser than tread, and that a deliberately steep flight fails exactly the three rules it should.

## Related calculators

- HOM-001 Concrete Calculator

## SEO metadata

- Title: `Stair Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/stair-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
