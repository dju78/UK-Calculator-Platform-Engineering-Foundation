# MAT-023 - Hex Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-023` |
| Name | Hex Calculator |
| Category | Maths & Algebra |
| Subcategory | Number Systems |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/hex-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Convert and compute in hexadecimal, with the other common bases shown alongside.

## Scope

Conversion and integer arithmetic in base 16.

## Assumptions

- Whole numbers only.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `operation` | Operation | select | "convert" | - |
| `value_a` | Hexadecimal value | text | "FF" | - |
| `value_b` | Second hexadecimal value | text | "" | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Same as MAT-022; the two share one implementation.

## Formula and logic

Same as MAT-022, at base 16.

## Outputs

- `decimal_result`
- `binary`
- `octal`
- `decimal`
- `hexadecimal`
- `bit_length`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Convert hexadecimal to decimal** - {"operation":"convert","value_a":"FF","value_b":""}
- **Hexadecimal addition** - {"operation":"add","value_a":"1A","value_b":"2B"}
- **Hexadecimal subtraction** - {"operation":"subtract","value_a":"FF","value_b":"A0"}
- **Hexadecimal multiplication** - {"operation":"multiply","value_a":"10","value_b":"10"}
- **A colour value** - {"operation":"convert","value_a":"7F3FBF","value_b":""}
- **Hexadecimal integer division** - {"operation":"divide","value_a":"100","value_b":"8"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Hexadecimal digits run 0 to 9 then A to F, so each carries exactly four bits, which is why it is the usual shorthand for binary data such as colours and memory addresses.

## Methodology

Shares the oracle with MAT-022, including a six-digit colour value.

## Related calculators

- MAT-022 Binary Calculator

## SEO metadata

- Title: `Hex Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/hex-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
