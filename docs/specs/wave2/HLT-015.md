# HLT-015 - One Rep Max Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `HLT-015` |
| Name | One Rep Max Calculator |
| Category | Health & Fitness |
| Subcategory | Exercise |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | medium |
| Rules-sensitive | No |
| Canonical route | `/calculators/one-rep-max-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Estimate a one repetition maximum from a submaximal set.

## Scope

A weight and a rep count up to ten.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `weight` | Weight lifted (kg) | number | 100 | - |
| `reps` | Repetitions completed | number | 5 | Up to 10. Above that the formulas stop being reliable. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Rep counts above ten are REFUSED, because the formulas diverge sharply there and stop being reliable.
- A whole number of at least one repetition is required.

## Formula and logic

Epley, Brzycki and Lombardi are all computed, and the LOWEST is used as the headline.

## Outputs

- `one_rep_max`
- `epley`
- `brzycki`
- `lombardi`
- `lowest_estimate`
- `highest_estimate`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Five repetitions** - {"weight":100,"reps":5}
- **A single repetition is already the maximum** - {"weight":120,"reps":1}
- **Three repetitions** - {"weight":110,"reps":3}
- **Eight repetitions** - {"weight":80,"reps":8}
- **Ten repetitions, the limit of reliability** - {"weight":70,"reps":10}
- **Two repetitions** - {"weight":115,"reps":2}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

The lowest estimate is deliberate: over-estimating a one rep max is how people get injured. These are estimates from a submaximal set, not a tested maximum, and a true maximum should never be attempted without a spotter and a proper warm-up.

## Methodology

A test asserts that the headline equals the minimum of the three and is strictly below the maximum, so a change to the most optimistic formula would fail.

## Related calculators

- HLT-016 Target Heart Rate

## SEO metadata

- Title: `One Rep Max Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/one-rep-max-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
