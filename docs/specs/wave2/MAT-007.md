# MAT-007 - Rounding Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-007` |
| Name | Rounding Calculator |
| Category | Maths & Algebra |
| Subcategory | Arithmetic |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/rounding-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

Round a number every common way at once, so the right convention can be chosen deliberately.

## Scope

One value, to decimal places, significant figures or the nearest multiple, under five rounding modes.

## Assumptions

- Decimal places and significant figures are within the range a double can represent.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `value` | Value | number | 3.14159 | - |
| `decimal_places` | Decimal places | number | 2 | - |
| `mode` | Rounding mode | select | "half_up" | - |
| `significant_figures` | Significant figures | number | 3 | - |
| `nearest_multiple` | Nearest multiple of | number | 0 | Leave at 0 to skip this. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Decimal places 0 to 15; significant figures 1 to 15.

## Formula and logic

Half up rounds an exact half away from zero. Half to even sends it to the nearer EVEN neighbour. Up, down and towards zero are the ceiling, floor and truncation.

## Outputs

- `result`
- `rounded_to_decimal_places`
- `rounded_to_significant_figures`
- `rounded_to_nearest_multiple`
- `rounded_up`
- `rounded_down`
- `truncated`
- `bankers_rounding`
- `difference_from_original`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Two decimal places** - {"value":3.14159,"decimal_places":2,"significant_figures":3,"nearest_multiple":0,"mode":"half_up"}
- **Exact half rounds up in the everyday mode** - {"value":2.5,"decimal_places":0,"significant_figures":2,"nearest_multiple":0,"mode":"half_up"}
- **Exact half goes to the even number in banker's rounding** - {"value":2.5,"decimal_places":0,"significant_figures":2,"nearest_multiple":0,"mode":"half_even"}
- **The other exact half also goes to the even number** - {"value":3.5,"decimal_places":0,"significant_figures":2,"nearest_multiple":0,"mode":"half_even"}
- **Always up** - {"value":4.01,"decimal_places":0,"significant_figures":3,"nearest_multiple":0,"mode":"up"}
- **Nearest multiple of 5** - {"value":47,"decimal_places":0,"significant_figures":2,"nearest_multiple":5,"mode":"half_up"}
- **Significant figures on a small number** - {"value":0.0004567,"decimal_places":6,"significant_figures":2,"nearest_multiple":0,"mode":"half_up"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Banker's rounding exists because always rounding halves upwards biases a long run of figures upwards. Accounting and statistics use half to even for that reason, while everyday arithmetic rounds half up. Both are shown rather than one being imposed.

## Methodology

Benchmarks include the matched pair 2.5 and 3.5, which banker's rounding sends to 2 and 4 respectively - in opposite directions - proving the rule is the even neighbour and not a fixed direction.

## Related calculators

- MAT-011 Scientific Notation
- MAT-003 Percentage Change

## SEO metadata

- Title: `Rounding Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/rounding-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
