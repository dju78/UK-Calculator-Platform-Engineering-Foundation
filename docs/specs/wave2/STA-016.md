# STA-016 - T-Test Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-016` |
| Name | T-Test Calculator |
| Category | Statistics & Data |
| Subcategory | Hypothesis Tests |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/t-test-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Compare means with the right t test, and report the effect size and interval alongside the p-value.

## Scope

One-sample, paired, and two independent samples with either pooled or Welch treatment.

## Assumptions

- The pooled two-sample test assumes equal variances; Welch does not.
- Observations are independent within groups.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `test_type` | Type of t test | select | "two_sample" | - |
| `sample_a` | Sample A | text | "12 | - |
| `sample_b` | Sample B | text | "20 | - |
| `hypothesised_mean` | Hypothesised mean | number | 0 | - |
| `confidence` | Confidence level | number | 0.95 | - |
| `two_tailed` | Two-tailed test? | select | "true" | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Each group needs at least two observations; a paired test needs matched lengths.
- Inputs are read by the shared dataset parser, which accepts commas, spaces, semicolons, new lines and JSON array notation, and REFUSES any token it cannot read rather than dropping it. Silently discarding an unreadable value would hand the user a confident answer to the wrong data.

## Formula and logic

The statistic is the difference over its standard error. Welch's degrees of freedom come from the Welch-Satterthwaite equation and are fractional by construction, which a test asserts. Cohen's d uses the pooled standard deviation.

## Outputs

- `t_statistic`
- `degrees_of_freedom`
- `p_value`
- `critical_value`
- `significant_at_5_percent`
- `mean_difference`
- `standard_error`
- `confidence_interval_lower`
- `confidence_interval_upper`
- `cohens_d`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **One sample against a hypothesised mean** - {"test_type":"one_sample","sample_a":"[5.1,4.9,5.3,5,4.8,5.2,5.4,4.7]","confidence":0.95,"two_tailed":true,"hypothesised_mean":5}
- **Two independent groups, pooled** - {"test_type":"two_sample","sample_a":"[12,14,15,16,18,13]","confidence":0.95,"two_tailed":true,"sample_b":"[20,22,19,24,21,23]"}
- **Unequal variances, so Welch is the right test** - {"test_type":"welch","sample_a":"[12,14,15,16,18,13]","confidence":0.95,"two_tailed":true,"sample_b":"[5,30,12,40,8,35]"}
- **Paired before and after** - {"test_type":"paired","sample_a":"[200,195,210,190,205,198]","confidence":0.95,"two_tailed":true,"sample_b":"[190,188,200,185,197,192]"}
- **No real difference between the groups** - {"test_type":"two_sample","sample_a":"[10,11,12,13,14]","confidence":0.95,"two_tailed":true,"sample_b":"[10,12,11,14,13]"}
- **Groups of different sizes** - {"test_type":"welch","sample_a":"[30,32,35,31]","confidence":0.95,"two_tailed":true,"sample_b":"[20,22,21,24,23,25,19,22]"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Welch is the safer choice whenever the groups differ in size or spread, and choosing the pooled test out of habit is a common error. The confidence interval and the effect size are always reported with the p-value, because a significant result on a trivial difference and a non-significant result on a large one are both routine. A statistical result on the data entered. Statistical significance is not the same as practical importance, and no test can tell you whether the data were collected in a way that supports the conclusion you want to draw.

## Methodology

Benchmarks come from an independent oracle that shares NO special-function code with the engine. Where the engine evaluates the t, chi-square and F distributions through the regularised incomplete beta and incomplete gamma functions, the oracle integrates each density numerically by Simpson's rule and obtains the normalising constant by integrating the unnormalised density over its whole support. No gamma function is used on that side at all, and infinite ranges are handled by substitution rather than truncation, so no tail is silently discarded. A benchmark pair on deliberately unequal variances shows the pooled and Welch degrees of freedom diverging sharply.

## Related calculators

- STA-007 P-Value
- STA-018 ANOVA
- STA-010 Statistical Power

## SEO metadata

- Title: `T-Test Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/t-test-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
