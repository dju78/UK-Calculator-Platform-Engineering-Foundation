# ISA-004 - Lifetime ISA Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `ISA-004` |
| Name | Lifetime ISA Calculator |
| Category | ISA & Tax Wrappers |
| Subcategory | ISA |
| Launch wave | Wave 2 |
| Jurisdiction | UK |
| Regulatory risk | high |
| Rules-sensitive | Yes |
| Canonical route | `/calculators/lifetime-isa-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Project a Lifetime ISA and show plainly what the 25% withdrawal charge costs, which is the single most misunderstood feature of the product.

## Scope

Contributions up to the annual Lifetime ISA limit, the 25% government bonus, growth to a chosen withdrawal date, and the withdrawal charge where the withdrawal does not qualify.

## Assumptions

- Contributions are made once a year and the bonus is credited at the same point.
- The bonus is 25% of the contribution, capped at the published annual maximum.
- Growth is applied after the contribution and bonus each year.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `current_balance` | Current Lifetime ISA balance (£) | number | 0 | - |
| `annual_contribution` | Annual contribution (£) | number | 4000 | The limit is £4,000 a year, and it counts towards your £20,000 overall ISA allowance. |
| `annual_growth` | Annual growth (%) | number | 5 | - |
| `years` | Years until withdrawal | number | 5 | - |
| `withdrawal_purpose` | What will you withdraw it for? | select | "first_home" | - |
| `property_price` | Property price (£) | number | "" | Only used for a first-home withdrawal. A home above £450,000 does not qualify, so the 25% charge would apply. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Contributions above the annual limit are capped, and the user is told they earn no bonus.
- A first-home withdrawal for a property above the price cap is treated as NOT charge free, because it is not.

## Formula and logic

Each year `v = (v + c + b) G`, where `c` is the capped contribution, `b` the bonus and `G` one plus the growth rate. The withdrawal charge is 25% of the amount withdrawn. Because the bonus was 25% of the contribution, a 25% charge on the larger post-bonus balance recovers the entire bonus and a further 6.25% of the saver's own money, which is reported as its own output.

## Outputs

- `total_contributions`
- `total_bonus`
- `final_value`
- `investment_growth`
- `withdrawal_charge`
- `net_withdrawal`
- `own_money_lost_to_charge`
- `charge_applies`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Maximum contributions, first home, within the price cap** - {"current_balance":0,"annual_contribution":4000,"annual_growth":5,"years":5,"withdrawal_purpose":"first_home","property_price":250000}
- **Maximum contributions, first home ABOVE the price cap so the charge applies** - {"current_balance":0,"annual_contribution":4000,"annual_growth":5,"years":5,"withdrawal_purpose":"first_home","property_price":500000}
- **Non-qualifying withdrawal shows the 6.25% loss of own money** - {"current_balance":0,"annual_contribution":4000,"annual_growth":0,"years":1,"withdrawal_purpose":"other","property_price":0}
- **Contribution above the limit is capped and earns no extra bonus** - {"current_balance":2000,"annual_contribution":6000,"annual_growth":4,"years":10,"withdrawal_purpose":"age_60","property_price":0}
- **Long run to age 60 with an opening balance** - {"current_balance":12000,"annual_contribution":3000,"annual_growth":6,"years":20,"withdrawal_purpose":"age_60","property_price":0}
- **Zero growth isolates contributions and bonus** - {"current_balance":0,"annual_contribution":2000,"annual_growth":0,"years":8,"withdrawal_purpose":"first_home","property_price":300000}

## Rules and source dependencies

Rules-sensitive. Every statutory figure is read from the versioned UK Rules Engine ruleset `uk-2026-27-v1`, which carries a source register naming the primary GOV.UK or HMRC page each value came from. No statutory value is hard-coded in a React component or in this calculator.

## Product boundary

A projection of one product on stated assumptions. Eligibility depends on age at opening and at contribution, and on the withdrawal reason; those rules are stated but not enforced against a date of birth. Not investment or financial advice.

## Methodology

Benchmarks were derived by an independent oracle that solves the same recurrence in closed form, where the engine iterates period by period. Neither side calls the other, and a shared iteration slip cannot pass unnoticed. One benchmark deliberately isolates the charge at zero growth over one year, so the 6.25% claim is proved arithmetically rather than asserted.

## Related calculators

- ISA-001 ISA Allowance
- ISA-003 ISA vs GIA
- PRO-013 First-Time Buyer

## SEO metadata

- Title: `Lifetime ISA Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/lifetime-isa-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
