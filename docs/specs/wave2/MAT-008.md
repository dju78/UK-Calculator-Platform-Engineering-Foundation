# MAT-008 - Exponent Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-008` |
| Name | Exponent Calculator |
| Category | Maths & Algebra |
| Subcategory | Algebra |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/exponent-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Raise a number to a power, including negative and fractional exponents.

## Scope

One base and one exponent.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `base` | Base | number | 2 | - |
| `exponent` | Exponent | number | 10 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Zero to a negative power is refused, because it is division by zero.
- A negative base with a fractional exponent is refused, because it has no real result - rather than being returned as a NaN the user has to interpret.

## Formula and logic

A negative exponent gives the reciprocal; any non-zero number to the power of zero is 1; a fractional exponent is a root.

## Outputs

- `result`
- `reciprocal`
- `squared`
- `cubed`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Square** - {"base":7,"exponent":2}
- **Negative exponent gives the reciprocal** - {"base":2,"exponent":-3}
- **Anything to the power of zero is one** - {"base":9.5,"exponent":0}
- **Fractional exponent is a root** - {"base":16,"exponent":0.5}
- **Negative base with a whole exponent** - {"base":-3,"exponent":3}
- **Large power** - {"base":1.05,"exponent":30}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Results beyond the range of a double are refused rather than reported as infinity.

## Methodology

Whole-number powers are checked in the oracle by REPEATED MULTIPLICATION rather than by an exponential function, so the two routes share nothing.

## Related calculators

- MAT-009 Root
- MAT-010 Log

## SEO metadata

- Title: `Exponent Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/exponent-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
