# AUT-007 - Fuel Economy Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `AUT-007` |
| Name | Fuel Economy Calculator |
| Category | Automotive & Travel |
| Subcategory | Running Cost |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/fuel-economy-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Turn a distance and a quantity of fuel into economy, in every unit anyone uses.

## Scope

A distance in miles or kilometres and fuel in litres or in either kind of gallon, with an optional price.

## Assumptions

- A single tankful is a sample of real driving, not a specification.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `distance` | Distance covered | number | 300 | - |
| `distance_unit` | Distance unit | select | "miles" | - |
| `fuel_used` | Fuel used | number | 30 | - |
| `fuel_unit` | Fuel unit | select | "litres" | A UK gallon is 4.546 litres; a US gallon is 3.785, which is why US mpg figures look worse. |
| `fuel_price_pence` | Fuel price (pence a litre) | number | 145 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- A zero or negative distance is refused, because there is no economy to measure.
- A zero or negative quantity of fuel is refused, because the economy would be infinite.
- A negative fuel price is refused.

## Formula and logic

Litres per mile is computed first and then inverted into each unit, so every figure comes from one quantity rather than from separate conversions that could drift apart.

## Outputs

- `mpg_imperial`
- `mpg_us`
- `litres_per_100km`
- `km_per_litre`
- `miles_per_litre`
- `fuel_used_litres`
- `distance_miles`
- `total_fuel_cost`
- `cost_per_mile_pence`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Three hundred miles on thirty litres** - {"distance":300,"distance_unit":"miles","fuel_used":30,"fuel_unit":"litres","fuel_price_pence":145}
- **A metric fill: five hundred kilometres on thirty-five litres** - {"distance":500,"distance_unit":"km","fuel_used":35,"fuel_unit":"litres","fuel_price_pence":149.9}
- **Quoted in imperial gallons** - {"distance":420,"distance_unit":"miles","fuel_used":9,"fuel_unit":"imperial_gallons","fuel_price_pence":142}
- **A US figure pasted in, which must not be treated as imperial** - {"distance":350,"distance_unit":"miles","fuel_used":10,"fuel_unit":"us_gallons","fuel_price_pence":145}
- **A thirsty vehicle over a short trip** - {"distance":60,"distance_unit":"miles","fuel_used":12,"fuel_unit":"litres","fuel_price_pence":152.9}
- **An efficient diesel on a long motorway run, no price given** - {"distance":620,"distance_unit":"miles","fuel_used":42,"fuel_unit":"litres","fuel_price_pence":null}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

THE HEADLINE IS IMPERIAL MPG. Miles per gallon means the IMPERIAL gallon of 4.54609 litres. The US gallon of 3.785411784 litres is a fifth smaller, so the same car reads about twenty per cent worse in US mpg: 50 mpg here is about 42 there. The constants live in the versioned ruleset rather than in the code, so no two calculators on the platform can disagree about them. The US figure is shown deliberately, not as a courtesy but because the two are constantly confused and a reader who has copied a figure from an American source needs to see the gap. LITRES PER 100 KM RUNS THE OTHER WAY: a lower number is better, and readers who treat it like mpg reach the opposite conclusion. Real economy varies with speed, load, weather and tyre pressure, so one tankful is an observation rather than a rating. Cost is reported in PENCE a mile, which is how running costs are compared in Britain and which keeps the precision that rounding pounds to two places would destroy.

## Methodology

The oracle re-types the gallon constants from their definitions rather than reading the ruleset, so agreement corroborates the ruleset rather than merely echoing it, and it derives economy through litres per mile inverted rather than by dividing miles by gallons. A US-gallon case is included so that treating a US figure as imperial would fail by twenty per cent rather than pass unnoticed.

## Related calculators

- AUT-006 Fuel Cost
- AUT-009 EV Charging Cost

## SEO metadata

- Title: `Fuel Economy Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/fuel-economy-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
