# GEO-004 - Triangle Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `GEO-004` |
| Name | Triangle Calculator |
| Category | Geometry |
| Subcategory | Geometry |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/triangle-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

Solve a triangle completely from its three sides.

## Scope

Any triangle given by three side lengths.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `side_a` | Side a | number | 6 | - |
| `side_b` | Side b | number | 7 | - |
| `side_c` | Side c | number | 8 | Any two sides must add up to more than the third. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- The TRIANGLE INEQUALITY is checked first: any two sides must add up to more than the third. Lengths that break it, including the degenerate collinear case where the sum is exactly equal, are refused with the offending lengths named.
- The argument of the inverse cosine is clamped, so floating-point drift can never push it outside the valid range.

## Formula and logic

Area by Heron's formula, angles by the cosine rule, and the inradius and circumradius from the area and the sides.

## Outputs

- `angle_a`
- `angle_b`
- `angle_c`
- `area`
- `perimeter`
- `semi_perimeter`
- `inradius`
- `circumradius`
- `height_to_a`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A 3-4-5 right triangle** - {"side_a":3,"side_b":4,"side_c":5}
- **Equilateral** - {"side_a":6,"side_b":6,"side_c":6}
- **Isosceles** - {"side_a":5,"side_b":5,"side_c":8}
- **Scalene and obtuse** - {"side_a":4,"side_b":5,"side_c":8}
- **Scalene and acute** - {"side_a":6,"side_b":7,"side_c":8}
- **A 5-12-13 right triangle** - {"side_a":5,"side_b":12,"side_c":13}
- **Nearly degenerate but still a triangle** - {"side_a":1,"side_b":1,"side_c":1.999}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Without the inequality check, Heron's formula returns a NaN or a phantom zero area for impossible lengths, either of which looks like an answer. Units are the user's own and are never converted: whatever unit the lengths are entered in, an area comes back in that unit squared and a volume in that unit cubed. The calculator states this rather than assuming metres, because a silent unit assumption is the easiest way to be wrong by a factor of a thousand.

## Methodology

The oracle computes the area BOTH by Heron's formula and by the cross product of the edge vectors placed in the plane, and the two must agree before a case is recorded. The angle sum is verified to be exactly 180 degrees.

## Related calculators

- GEO-005 Right Triangle
- GEO-007 Pythagorean Theorem

## SEO metadata

- Title: `Triangle Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/triangle-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
