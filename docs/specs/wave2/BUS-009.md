# BUS-009 - Depreciation Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `BUS-009` |
| Name | Depreciation Calculator |
| Category | Business & Commercial |
| Subcategory | Accounting |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/depreciation-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

Produce a depreciation schedule under any of the four common methods.

## Scope

One asset: cost, residual value, useful life, and the method, with a full year-by-year schedule.

## Assumptions

- Depreciation is charged annually.
- Under units of production, the units for each year are supplied.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `cost` | Asset cost (£) | number | 25000 | - |
| `residual_value` | Residual value (£) | number | 5000 | What you expect it to be worth at the end of its useful life. |
| `useful_life_years` | Useful life (years) | number | 5 | - |
| `method` | Method | select | "straight_line" | - |
| `reducing_balance_rate` | Reducing balance rate (%) | number | 25 | - |
| `total_units` | Total expected units | number | 0 | - |
| `units_per_year` | Units each year | text | "[]" | A list, for example [30000, 30000, 25000, 15000]. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- The residual value cannot exceed the cost.
- The useful life must be at least one year.
- Total expected units must be above zero when the units of production method is chosen.
- Every charge is floored so the book value can never fall below the residual value.

## Formula and logic

Straight line charges the depreciable amount evenly. Reducing balance charges a fixed percentage of the opening book value. Sum of the years' digits weights the charge towards the early years by `(remaining life) / (sum of digits)`. Units of production apportions by output.

## Outputs

- `depreciable_amount`
- `first_year_depreciation`
- `final_year_depreciation`
- `total_depreciation`
- `average_annual_depreciation`
- `closing_book_value`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Straight line over five years** - {"cost":25000,"residual_value":5000,"useful_life_years":5,"method":"straight_line"}
- **Reducing balance at 25%** - {"cost":25000,"residual_value":5000,"useful_life_years":5,"method":"reducing_balance","reducing_balance_rate":25}
- **Reducing balance floored at the residual value** - {"cost":10000,"residual_value":4000,"useful_life_years":10,"method":"reducing_balance","reducing_balance_rate":40}
- **Sum of the years' digits front-loads the charge** - {"cost":25000,"residual_value":5000,"useful_life_years":5,"method":"sum_of_years_digits"}
- **Units of production** - {"cost":60000,"residual_value":10000,"useful_life_years":4,"method":"units_of_production","total_units":100000,"units_per_year":"[30000,30000,25000,15000]"}
- **No residual value** - {"cost":12000,"residual_value":0,"useful_life_years":3,"method":"straight_line"}
- **Single-year life** - {"cost":5000,"residual_value":1000,"useful_life_years":1,"method":"straight_line"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

This is accounting depreciation for management purposes, NOT capital allowances. HMRC does not accept depreciation as a deductible expense, and the tax relief actually available comes through capital allowances instead - a distinction the calculator states rather than leaves the user to discover. A management estimate on the figures entered. It is not a statutory account, a tax computation, or accounting advice, and it follows no particular accounting standard.

## Methodology

Benchmarks were derived by an independent year-by-year simulation, and include a 40% reducing balance over ten years specifically to prove the charge is floored at the residual value rather than running away below scrap.

## Related calculators

- BUS-005 Net Profit
- TAX-016 Self-Employment Tax
- AUT-011 Vehicle Depreciation

## SEO metadata

- Title: `Depreciation Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/depreciation-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
