# ISA-003 - ISA vs GIA Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `ISA-003` |
| Name | ISA vs GIA Calculator |
| Category | ISA & Tax Wrappers |
| Subcategory | ISA |
| Launch wave | Wave 2 |
| Jurisdiction | UK |
| Regulatory risk | high |
| Rules-sensitive | Yes |
| Canonical route | `/calculators/isa-vs-gia-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Show how much of a portfolio's return is lost to tax when it is held outside an ISA, so the value of the wrapper is a number rather than a slogan.

## Scope

A single portfolio with capital growth and a dividend yield, held for a fixed term and realised at the end, compared inside a stocks and shares ISA and inside a taxable general investment account.

## Assumptions

- The portfolio itself performs identically in both wrappers; only the tax differs.
- Dividends are reinvested each month and therefore raise the cost basis of the taxable holding.
- Dividend tax on the taxable account is settled from money outside the portfolio.
- The whole gain is realised in a single tax year at the end of the term.
- The dividend allowance and the capital gains annual exempt amount are entirely available to this holding.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `initial_investment` | Initial investment (£) | number | 10000 | - |
| `monthly_contribution` | Monthly contribution (£) | number | 200 | - |
| `annual_growth` | Annual capital growth (%) | number | 6 | Enter 6 for 6%. Growth excluding dividends. |
| `dividend_yield` | Dividend yield (%) | number | 2 | The income the holding pays out each year, as a percentage of its value. |
| `years` | Years held | number | 10 | - |
| `other_income` | Your other annual income (£) | number | 35000 | Used to work out your dividend and capital gains rates. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- The projection warns when contributions exceed the annual ISA subscription limit, because that much could not actually be sheltered in one tax year.

## Formula and logic

The holding follows `v_k = v_{k-1} (1 + g_m)(1 + d_m) + c` each month, where `g_m` is the monthly capital growth, `d_m` the monthly dividend yield and `c` the contribution. The cost basis rises by contributions and by reinvested dividends. Dividend tax is charged once every twelve months on the year's dividends above the allowance. Capital gains tax is charged on the final gain above the annual exempt amount, split between the 18% and 24% rates by how much basic rate band the investor has left.

## Outputs

- `final_gross_value`
- `total_contributions`
- `total_dividends`
- `cost_basis`
- `capital_gain`
- `isa_net_proceeds`
- `gia_dividend_tax`
- `gia_capital_gains_tax`
- `gia_net_proceeds`
- `isa_advantage`
- `years_dividend_allowance_exceeded`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Basic-rate saver, ten years, modest yield** - {"initial_investment":10000,"monthly_contribution":200,"annual_growth":6,"dividend_yield":2,"years":10,"other_income":35000}
- **Higher-rate saver, twenty years** - {"initial_investment":20000,"monthly_contribution":500,"annual_growth":7,"dividend_yield":3,"years":20,"other_income":70000}
- **Lump sum only, no contributions** - {"initial_investment":50000,"monthly_contribution":0,"annual_growth":5,"dividend_yield":4,"years":15,"other_income":45000}
- **No dividend yield, so the ISA advantage is capital gains only** - {"initial_investment":15000,"monthly_contribution":300,"annual_growth":8,"dividend_yield":0,"years":12,"other_income":60000}
- **Additional-rate saver** - {"initial_investment":100000,"monthly_contribution":1000,"annual_growth":6,"dividend_yield":3.5,"years":10,"other_income":200000}
- **Short term where the gain stays inside the annual exempt amount** - {"initial_investment":5000,"monthly_contribution":50,"annual_growth":4,"dividend_yield":1,"years":3,"other_income":30000}

## Rules and source dependencies

Rules-sensitive. Every statutory figure is read from the versioned UK Rules Engine ruleset `uk-2026-27-v1`, which carries a source register naming the primary GOV.UK or HMRC page each value came from. No statutory value is hard-coded in a React component or in this calculator.

## Product boundary

A projection, not a recommendation between two products. Selling units to pay the dividend tax would itself be a disposal and create a further gain, which is deliberately not modelled; the tax is shown as a cost instead. Not investment advice.

## Methodology

Benchmarks were derived by an independent oracle that solves the same recurrence in closed form, where the engine iterates period by period. Neither side calls the other, and a shared iteration slip cannot pass unnoticed.

## Related calculators

- ISA-001 ISA Allowance
- ISA-006 Cash ISA
- TAX-011 Dividend Tax
- TAX-012 Capital Gains Tax

## SEO metadata

- Title: `ISA vs GIA Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/isa-vs-gia-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
