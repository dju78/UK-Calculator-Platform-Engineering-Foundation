# MAT-013 - Slope Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-013` |
| Name | Slope Calculator |
| Category | Maths & Algebra |
| Subcategory | Algebra |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/slope-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Describe the line through two points: its gradient, equation, length and midpoint.

## Scope

Two points in the plane.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `x1` | x1 | number | 1 | - |
| `y1` | y1 | number | 2 | - |
| `x2` | x2 | number | 4 | - |
| `y2` | y2 | number | 8 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Two identical points are refused, because they do not define a line.

## Formula and logic

Slope is the change in y over the change in x. The angle is measured from the positive x axis. Distance is Pythagorean.

## Outputs

- `angle_degrees`
- `distance`
- `midpoint_x`
- `midpoint_y`
- `slope`
- `y_intercept`
- `perpendicular_slope`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Positive slope** - {"x1":1,"y1":2,"x2":4,"y2":8}
- **Negative slope** - {"x1":0,"y1":10,"x2":5,"y2":0}
- **Horizontal line** - {"x1":-3,"y1":4,"x2":6,"y2":4}
- **Vertical line has no slope** - {"x1":2,"y1":1,"x2":2,"y2":9}
- **Slope of exactly one** - {"x1":0,"y1":0,"x2":5,"y2":5}
- **Fractional slope** - {"x1":1,"y1":1,"x2":4,"y2":2}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

A VERTICAL line has no slope at all, which is different from a slope of zero. It is reported as such, with the equation in the form x = a, rather than dividing by zero.

## Methodology

Independently derived; the vertical case asserts only the quantities that exist for it.

## Related calculators

- MAT-012 Quadratic Formula
- GEO-008 Distance

## SEO metadata

- Title: `Slope Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/slope-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
