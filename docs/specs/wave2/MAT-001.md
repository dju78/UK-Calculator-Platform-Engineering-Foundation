# MAT-001 - Basic Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-001` |
| Name | Basic Calculator |
| Category | Maths & Algebra |
| Subcategory | Arithmetic |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/basic-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Evaluate an arithmetic expression with the standard order of operations.

## Scope

Arithmetic, powers, brackets and the common mathematical functions.

## Assumptions

- Angles are in radians unless a degree function is used explicitly.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `expression` | Expression | text | "2 + 3 * 4" | Use + - * / ^ and brackets. Functions such as sqrt, sin and log are available. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Expressions are capped in length.
- The expression language's own extension features - import, createUnit, a nested evaluate, and the symbolic helpers - are DISABLED inside expressions. A calculator needs none of them, and leaving them reachable is the whole of the expression-injection problem.

## Formula and logic

Brackets, then powers, then multiplication and division, then addition and subtraction, evaluated left to right within each level.

## Outputs

- `result`
- `is_whole_number`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Order of operations** - {"expression":"2 + 3 * 4"}
- **Brackets override precedence** - {"expression":"(2 + 3) * 4"}
- **Powers bind tighter than multiplication** - {"expression":"2 * 3 ^ 2"}
- **Division and subtraction** - {"expression":"100 / 4 - 5"}
- **Nested brackets** - {"expression":"((1 + 2) * (3 + 4)) / 7"}
- **Decimal arithmetic** - {"expression":"0.1 * 3 + 0.7"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

This evaluates arithmetic, not algebra: it will not solve for a variable or simplify symbolically.

## Methodology

Benchmark expectations were worked out by hand from the order of operations, not by running any evaluator.

## Related calculators

- MAT-002 Scientific Calculator
- MAT-012 Quadratic Formula

## SEO metadata

- Title: `Basic Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/basic-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
