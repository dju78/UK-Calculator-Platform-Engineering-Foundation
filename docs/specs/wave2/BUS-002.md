# BUS-002 - Markup Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `BUS-002` |
| Name | Markup Calculator |
| Category | Business & Commercial |
| Subcategory | Pricing |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/markup-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

Convert between cost, price, markup and margin, and show both percentages side by side so they cannot be confused.

## Scope

One product: a cost with either a selling price or a markup percentage.

## Assumptions

- Cost is the full cost of the item being priced.
- Percentages are entered as human percentages.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `cost` | Unit cost (£) | number | 100 | - |
| `price` | Selling price (£) | number | "" | Enter either a selling price or a markup percentage below. |
| `markup_percentage` | Markup (%) | number | "" | Used only when no selling price is entered. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Cost must be greater than zero, because a markup on nothing is undefined.
- Either a price or a markup percentage must be supplied; the calculator says so rather than guessing.

## Formula and logic

Markup is profit divided by COST. Margin is profit divided by PRICE. Where a markup percentage is supplied instead of a price, the price is `cost x (1 + markup)`.

## Outputs

- `cost`
- `price`
- `profit`
- `markup`
- `margin`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A 50% markup is a 33.3% margin, not a 50% one** - {"cost":100,"price":150,"markup_percentage":""}
- **Price derived from a markup percentage** - {"cost":80,"price":"","markup_percentage":25}
- **Doubling the cost is a 100% markup and a 50% margin** - {"cost":40,"price":80,"markup_percentage":""}
- **Thin markup on a high-value item** - {"cost":9500,"price":9900,"markup_percentage":""}
- **Selling at cost gives no markup and no margin** - {"cost":250,"price":250,"markup_percentage":""}
- **Selling below cost gives a negative markup** - {"cost":100,"price":85,"markup_percentage":""}
- **Large markup from a percentage** - {"cost":12.5,"price":"","markup_percentage":300}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Markup and margin are never the same number, and mixing them up is the most common pricing error in a small business: a 50% markup is a 33.3% margin. Both are therefore always returned together, never one alone.

## Methodology

Benchmarks were derived by direct independent arithmetic, and every case asserts BOTH markup and margin, so the two could not be swapped without failing.

## Related calculators

- BUS-001 Margin Calculator
- BUS-011 Pricing
- BUS-004 Gross Profit

## SEO metadata

- Title: `Markup Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/markup-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
