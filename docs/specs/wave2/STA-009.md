# STA-009 - Margin of Error Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-009` |
| Name | Margin of Error Calculator |
| Category | Statistics & Data |
| Subcategory | Research |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/margin-of-error-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Work out the margin of error on a survey result, and what a tighter one would cost.

## Scope

A proportion or a mean, a sample size, a confidence level and an optional population size.

## Assumptions

- The sample was drawn at random.
- The allowances cover sampling error only.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `measure` | What are you measuring? | select | "proportion" | - |
| `proportion` | Proportion | number | 0.5 | As a decimal. Use 0.5 for the widest, most cautious margin. |
| `sample_mean` | Sample mean | number | 0 | - |
| `standard_deviation` | Standard deviation | number | 0 | - |
| `sample_size` | Sample size | number | 1000 | - |
| `confidence` | Confidence level | number | 0.95 | As a decimal, for example 0.95 for 95%. |
| `population_size` | Population size (optional) | number | "" | Only needed when your sample is a large share of the whole group. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- The sample size must be at least 1 and the confidence level strictly between 0 and 1.
- A proportion must be between 0 and 1.

## Formula and logic

The margin is the critical z times the standard error. The finite population correction is applied only where the sample exceeds 5% of the population, because below that it changes nothing worth reporting.

## Outputs

- `margin_of_error`
- `lower_bound`
- `upper_bound`
- `critical_value`
- `standard_error`
- `sample_size_for_half_the_margin`
- `finite_population_correction_applied`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Opinion poll of 1,000 at 95%** - {"measure":"proportion","sample_size":1000,"confidence":0.95,"population_size":"","proportion":0.5}
- **Poll of 400 with a lopsided proportion** - {"measure":"proportion","sample_size":400,"confidence":0.95,"population_size":"","proportion":0.8}
- **Mean with a known standard deviation** - {"measure":"mean","sample_size":100,"confidence":0.95,"population_size":"","sample_mean":100,"standard_deviation":15}
- **99% confidence widens the interval** - {"measure":"proportion","sample_size":1000,"confidence":0.99,"population_size":"","proportion":0.5}
- **Staff survey where the sample is a large share of the population** - {"measure":"proportion","sample_size":300,"confidence":0.95,"population_size":500,"proportion":0.6}
- **Population large enough that the correction does not apply** - {"measure":"proportion","sample_size":300,"confidence":0.95,"population_size":100000,"proportion":0.6}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

The margin covers SAMPLING error only. It says nothing about a biased sample, a leading question, or the people who declined to answer - which are usually larger sources of error than the one it measures. Halving the margin takes four times the sample, not twice, and that figure is reported explicitly because it is the most useful fact when sizing a survey.

## Methodology

Benchmarks come from an independent oracle that shares NO special-function code with the engine. Where the engine evaluates the t, chi-square and F distributions through the regularised incomplete beta and incomplete gamma functions, the oracle integrates each density numerically by Simpson's rule and obtains the normalising constant by integrating the unnormalised density over its whole support. No gamma function is used on that side at all, and infinite ranges are handled by substitution rather than truncation, so no tail is silently discarded. A matched pair differing only in population size proves the correction is applied when the sample is a large share of the group and not otherwise.

## Related calculators

- STA-008 Sample Size
- STA-006 Confidence Interval
- STA-019 A/B Test

## SEO metadata

- Title: `Margin of Error Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/margin-of-error-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
