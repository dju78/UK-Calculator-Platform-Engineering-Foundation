# HOM-005 - Mulch Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `HOM-005` |
| Name | Mulch Calculator |
| Category | Home & Construction |
| Subcategory | Garden |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/mulch-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Work out the mulch a bed needs, in the bags and bulk bags it is actually sold in.

## Scope

An area and a depth, with the supplier's own bulk density.

## Assumptions

- An even depth.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `length_m` | Length (m) | number | 6 | - |
| `width_m` | Width (m) | number | 3 | - |
| `area_m2` | Or enter the area directly (m²) | number | "" | - |
| `depth_mm` | Depth (mm) | number | 75 | 50 to 75 mm is the usual range: thinner and weeds come through, much thicker and it sheds rain. |
| `bulk_density` | Bulk density (kg/m³) | number | 350 | Bark mulch is light, around 250 to 400. |
| `bag_size_litres` | Bag size (litres) | number | 70 | - |
| `bulk_bag_m3` | Bulk bag size (m³) | number | 1 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- As HOM-004: implausible depths, densities and areas are refused.

## Formula and logic

As HOM-004, with packaging in litres because that is how mulch is sold.

## Outputs

- `area_m2`
- `depth_mm`
- `volume_m3`
- `weight_tonnes`
- `bags_needed`
- `bulk_bags_needed`
- `bulk_density_kg_per_m3`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A border at the recommended 75 mm** - {"area_m2":null,"length_m":6,"width_m":3,"depth_mm":75,"bulk_density":350,"bag_size_litres":70,"bulk_bag_m3":1}
- **A thinner 50 mm layer** - {"area_m2":null,"length_m":10,"width_m":2,"depth_mm":50,"bulk_density":300,"bag_size_litres":70,"bulk_bag_m3":1}
- **Heavier composted bark** - {"area_m2":null,"length_m":4,"width_m":4,"depth_mm":75,"bulk_density":400,"bag_size_litres":50,"bulk_bag_m3":1}
- **A very light wood chip** - {"area_m2":null,"length_m":12,"width_m":2.5,"depth_mm":60,"bulk_density":250,"bag_size_litres":70,"bulk_bag_m3":1}
- **An area given directly** - {"area_m2":45,"length_m":0,"width_m":0,"depth_mm":75,"bulk_density":350,"bag_size_litres":70,"bulk_bag_m3":1}
- **A small bed needing a single bag** - {"area_m2":null,"length_m":1.5,"width_m":0.6,"depth_mm":50,"bulk_density":350,"bag_size_litres":70,"bulk_bag_m3":1}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

MULCH IS SOLD BY VOLUME, NOT BY WEIGHT, which is the opposite of gravel, and bark is light: around 250 to 400 kilograms a cubic metre against gravel's 1,500. Litres are therefore the useful unit here and the weight is reported mainly so a delivery can be checked against a docket. THE BULK DENSITY IS AN INPUT BECAUSE IT IS NOT USEFULLY A PROPERTY OF THE MATERIAL. It moves with particle size, with moisture, and with whether the material is loose or compacted. Suppliers sell by weight while the job is measured by volume, so this single number decides whether a delivery is enough, and one published figure would be wrong for most orders by enough to matter. A DEPTH OF 50 TO 75 MM IS THE USUAL RANGE. Thinner and weeds come through, which is the main reason for mulching at all; much thicker and the mulch can shed rain rather than hold moisture in. Keep it clear of stems and trunks, because piling mulch against bark keeps it wet and invites rot.

## Methodology

Shares the oracle route with HOM-004, computing in litres first. A unit test asserts that gravel and mulch give an identical volume for the same bed and a weight differing by more than fourfold, which is the whole practical point of separating the two calculators.

## Related calculators

- HOM-004 Gravel Calculator

## SEO metadata

- Title: `Mulch Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/mulch-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
