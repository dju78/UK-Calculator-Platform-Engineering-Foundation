# STA-020 - Percent Error Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-020` |
| Name | Percent Error Calculator |
| Category | Statistics & Data |
| Subcategory | Research |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/percent-error-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Measure how far an observation falls from the value it should have taken.

## Scope

One observed value against one expected value.

## Assumptions

- The expected value is the reference, not the observation.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `observed` | Observed value | number | 10.5 | - |
| `expected` | Expected value | number | 10 | The error is measured against this value. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- A zero expected value is refused, because there is nothing to measure the error against.

## Formula and logic

The error is the observation less the expected value, divided by the ABSOLUTE expected value so a negative reference does not flip the sign. Both the absolute and the signed percentage are returned.

## Outputs

- `absolute_error`
- `percent_error`
- `relative_error`
- `signed_percent_error`
- `direction`
- `within_one_percent`
- `within_five_percent`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Measurement over the expected value** - {"observed":10.5,"expected":10}
- **Measurement under the expected value** - {"observed":9.2,"expected":10}
- **Exact measurement** - {"observed":25,"expected":25}
- **Large error** - {"observed":150,"expected":100}
- **Negative expected value** - {"observed":-8,"expected":-10}
- **Within one per cent** - {"observed":100.5,"expected":100}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Percent error is measured against the expected value and not the observed one; dividing by the observation instead is a common mistake that produces a different number.

## Methodology

Benchmarks were derived independently and include a negative expected value, which proves the denominator is taken as an absolute value.

## Related calculators

- STA-005 Z-Score
- MAT-003 Percentage Change

## SEO metadata

- Title: `Percent Error Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/percent-error-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
