# TEC-003 - Base64 Encode / Decode

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `TEC-003` |
| Name | Base64 Encode / Decode |
| Category | Technology & Digital |
| Subcategory | Encoding |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/base64-encode-decode` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 12 |

## Purpose

Encode and decode Base64, including the URL-safe variant.

## Scope

Text of up to 100,000 characters, in either direction.

## Assumptions

- Text is UTF-8.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `direction` | Encode or decode? | select | "encode" | - |
| `text` | Text | text | "Hello | - |
| `url_safe` | Use the URL-safe alphabet? | select | "false" | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- An invalid Base64 character is refused BY NAME with the valid set listed.
- Bytes that do not decode to valid text are refused with an explanation that the input may be binary.
- Oversized inputs are refused.

## Formula and logic

Every three bytes become four characters from a 64-character alphabet, with equals signs padding the final group.

## Outputs

- `output`
- `input_length`
- `output_length`
- `padding_characters`
- `size_change_pct`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Plain ASCII** - {"direction":"encode","text":"Hello, world!","url_safe":false}
- **Text with non-ASCII characters, which must survive as UTF-8** - {"direction":"encode","text":"Hello, wörld!","url_safe":false}
- **A length that needs two padding characters** - {"direction":"encode","text":"a","url_safe":false}
- **A length that needs one padding character** - {"direction":"encode","text":"ab","url_safe":false}
- **A length that needs no padding at all** - {"direction":"encode","text":"abc","url_safe":false}
- **The URL-safe alphabet, with padding stripped** - {"direction":"encode","text":"subjects?_d=1&x=~","url_safe":true}
- **Decoding plain ASCII** - {"direction":"decode","text":"SGVsbG8sIHdvcmxkIQ==","url_safe":false}
- **Decoding UTF-8 with an accented character** - {"direction":"decode","text":"SGVsbG8sIHfDtnJsZCE=","url_safe":false}
- **Decoding with no padding needed** - {"direction":"decode","text":"YWJj","url_safe":false}
- **Decoding the URL-safe alphabet** - {"direction":"decode","text":"c3ViamVjdHM_X2Q9MSZ4PX4","url_safe":false}
- **Decoding a single character** - {"direction":"decode","text":"YQ==","url_safe":false}
- **Decoding a longer passage** - {"direction":"decode","text":"VGhlIHF1aWNrIGJyb3duIGZveCBqdW1wcyBvdmVyIHRoZSBsYXp5IGRvZw==","url_safe":false}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

BASE64 IS AN ENCODING, NOT ENCRYPTION. It is trivially reversible by anyone, provides no confidentiality whatsoever, and a credential that has been Base64 encoded is exactly as exposed as one that has not. Its purpose is to carry arbitrary BYTES through a channel that only reliably handles text, such as an email body or a JSON string. This is worth saying plainly because Base64 in a configuration file is routinely mistaken for a protected secret. IT MAKES DATA ABOUT A THIRD LARGER, and the equals signs at the end are padding rather than data. TEXT IS ENCODED AS UTF-8 FIRST, so accented and non-Latin characters survive the round trip; a byte-per-character implementation mangles them, and round-trip tests here include Japanese and a euro sign for that reason. The implementation is written out from the alphabet rather than delegated to a platform helper, so the behaviour is identical wherever it runs.

## Methodology

The oracle encodes by an explicit BIT-STRING construction: each byte is written out as binary text, the string is regrouped into sixes and each group looked up. That is the definition of the encoding rather than an optimised form of it, so a shift or mask error in the engine's arithmetic version could not be reproduced. One, two and three character cases pin all three padding outcomes.

## Related calculators

- TEC-004 URL Encode / Decode

## SEO metadata

- Title: `Base64 Encode / Decode | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/base64-encode-decode`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (12)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
