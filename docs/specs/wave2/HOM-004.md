# HOM-004 - Gravel Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `HOM-004` |
| Name | Gravel Calculator |
| Category | Home & Construction |
| Subcategory | Construction |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/gravel-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Work out the gravel a drive or path needs, by volume, weight and packaging.

## Scope

An area and a depth, with the supplier's own bulk density.

## Assumptions

- An even depth over a prepared base.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `length_m` | Length (m) | number | 10 | - |
| `width_m` | Width (m) | number | 4 | - |
| `area_m2` | Or enter the area directly (m²) | number | "" | - |
| `depth_mm` | Depth (mm) | number | 50 | About 50 mm over a compacted sub-base is usual for a drive. |
| `bulk_density` | Bulk density (kg/m³) | number | 1500 | Gravel runs about 1,400 to 1,700 depending on stone size and moisture. Use your supplier's figure. |
| `bag_size_litres` | Bag size (litres) | number | 25 | - |
| `bulk_bag_m3` | Bulk bag size (m³) | number | 0.5 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- A depth above one metre is refused with a reminder that depths are in millimetres.
- A bulk density above 3,000 kg per cubic metre is refused as denser than solid rock.
- Areas above ten hectares are refused.

## Formula and logic

Volume is the area times the depth. Weight is the volume times the bulk density. Coverage per tonne is the inverse.

## Outputs

- `area_m2`
- `depth_mm`
- `volume_m3`
- `weight_tonnes`
- `bags_needed`
- `bulk_bags_needed`
- `bulk_density_kg_per_m3`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A drive at the usual 50 mm depth** - {"area_m2":null,"length_m":10,"width_m":4,"depth_mm":50,"bulk_density":1500,"bag_size_litres":25,"bulk_bag_m3":0.5}
- **A path in coarser, denser stone** - {"area_m2":null,"length_m":20,"width_m":1.2,"depth_mm":60,"bulk_density":1700,"bag_size_litres":25,"bulk_bag_m3":0.85}
- **A large area given directly** - {"area_m2":250,"length_m":0,"width_m":0,"depth_mm":50,"bulk_density":1500,"bag_size_litres":25,"bulk_bag_m3":0.5}
- **Light decorative aggregate** - {"area_m2":null,"length_m":6,"width_m":3,"depth_mm":40,"bulk_density":1400,"bag_size_litres":20,"bulk_bag_m3":0.5}
- **A deep sub-base layer** - {"area_m2":null,"length_m":8,"width_m":5,"depth_mm":150,"bulk_density":1600,"bag_size_litres":25,"bulk_bag_m3":0.85}
- **A very small area, where one bulk bag is still the minimum** - {"area_m2":null,"length_m":1,"width_m":1,"depth_mm":50,"bulk_density":1500,"bag_size_litres":25,"bulk_bag_m3":0.5}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

THE BULK DENSITY IS AN INPUT BECAUSE IT IS NOT USEFULLY A PROPERTY OF THE MATERIAL. It moves with particle size, with moisture, and with whether the material is loose or compacted. Suppliers sell by weight while the job is measured by volume, so this single number decides whether a delivery is enough, and one published figure would be wrong for most orders by enough to matter. Gravel typically runs between about 1.4 and 1.7 tonnes a cubic metre depending on stone size and moisture. THE COVERAGE PER TONNE IS THE FIGURE TO CHECK A QUOTE AGAINST, because a supplier prices by the tonne while the job is measured in area and depth, and that conversion is where an order goes wrong. For a drive, around 50 mm over a properly prepared and compacted sub-base is usual. Deeper gravel is harder to walk and drive on rather than better, because vehicles rut it.

## Methodology

The oracle computes volume in LITRES first, using the identity that a square metre at one millimetre deep is exactly one litre, then converts. That is both a different route from the engine's metres throughout and the arithmetic a builders' merchant does in their head. A unit test asserts that coverage per tonne multiplied by the tonnage recovers the area exactly.

## Related calculators

- HOM-005 Mulch Calculator
- HOM-001 Concrete Calculator

## SEO metadata

- Title: `Gravel Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/gravel-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
