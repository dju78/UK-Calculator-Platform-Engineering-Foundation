# TAX-008 - Overtime Pay Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `TAX-008` |
| Name | Overtime Pay Calculator |
| Category | UK Tax & Salary |
| Subcategory | Salary |
| Launch wave | Wave 2 |
| Jurisdiction | UK |
| Regulatory risk | medium |
| Rules-sensitive | No |
| Canonical route | `/calculators/overtime-pay-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Work out gross pay for a period that includes overtime at one or two premium rates, and the blended rate that results.

## Scope

One pay period: standard hours at the base rate, overtime hours at a multiplier, and an optional second premium tier such as double time.

## Assumptions

- All hours in a tier are paid at that tier's rate.
- The annualised figure assumes every pay period looks like this one, which overtime rarely does.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `base_hourly_rate` | Base hourly rate (£) | number | 15 | - |
| `standard_hours` | Standard hours this period | number | 37.5 | - |
| `overtime_hours` | Overtime hours | number | 8 | - |
| `overtime_multiplier` | Overtime multiplier | number | 1.5 | 1.5 for time and a half. |
| `premium_hours` | Premium-rate hours | number | 0 | - |
| `premium_multiplier` | Premium multiplier | number | 2 | 2 for double time. |
| `pay_periods_per_year` | Pay periods per year | number | 52 | 52 if you are paid weekly, 12 if monthly. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Negative hours are rejected.
- A multiplier below 1 is rejected, because it would pay less than the standard rate and is almost always a typing error.

## Formula and logic

`total = rate x standard + rate x overtime multiplier x overtime hours + rate x premium multiplier x premium hours`. The blended rate is the total divided by all hours worked.

## Outputs

- `base_pay`
- `overtime_pay`
- `premium_overtime_pay`
- `total_pay`
- `total_hours`
- `blended_hourly_rate`
- `overtime_hourly_rate`
- `premium_hourly_rate`
- `annualised_total`
- `overtime_share_of_pay`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Time and a half on eight hours** - {"base_hourly_rate":15,"standard_hours":37.5,"overtime_hours":8,"overtime_multiplier":1.5,"premium_hours":0,"premium_multiplier":2,"pay_periods_per_year":52}
- **Time and a half plus double time** - {"base_hourly_rate":12,"standard_hours":40,"overtime_hours":6,"overtime_multiplier":1.5,"premium_hours":4,"premium_multiplier":2,"pay_periods_per_year":52}
- **No overtime worked** - {"base_hourly_rate":20,"standard_hours":35,"overtime_hours":0,"overtime_multiplier":1.5,"premium_hours":0,"premium_multiplier":2,"pay_periods_per_year":52}
- **Overtime at plain time** - {"base_hourly_rate":11.5,"standard_hours":37.5,"overtime_hours":10,"overtime_multiplier":1,"premium_hours":0,"premium_multiplier":2,"pay_periods_per_year":52}
- **Monthly paid, heavy overtime** - {"base_hourly_rate":18,"standard_hours":160,"overtime_hours":30,"overtime_multiplier":1.5,"premium_hours":10,"premium_multiplier":2,"pay_periods_per_year":12}
- **Double time only** - {"base_hourly_rate":25,"standard_hours":30,"overtime_hours":0,"overtime_multiplier":1.5,"premium_hours":12,"premium_multiplier":2,"pay_periods_per_year":52}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Gross pay before deductions. There is no legal right to a higher rate for overtime in the UK unless the contract provides one, and average pay across all hours worked must still meet the National Minimum Wage.

## Methodology

Benchmarks were derived by direct arithmetic independent of the engine, covering plain-time overtime, a single premium tier, both tiers together, and no overtime at all.

## Related calculators

- TAX-006 Hourly to Salary
- TAX-002 Salary Calculator

## SEO metadata

- Title: `Overtime Pay Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/overtime-pay-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
