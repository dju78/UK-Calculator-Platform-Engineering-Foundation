# SCI-003 - Electricity Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `SCI-003` |
| Name | Electricity Calculator |
| Category | Science & Engineering |
| Subcategory | Electrical |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/electricity-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Cost running an appliance, separating the energy from the standing charge.

## Scope

An appliance power, a usage pattern, a unit rate and a standing charge.

## Assumptions

- The appliance draws its rated power for the whole time it is on.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `power_watts` | Appliance power (W) | number | 2000 | - |
| `hours_per_day` | Hours used a day | number | 1 | - |
| `days` | Over how many days? | number | 365 | - |
| `price_pence_per_kwh` | Unit rate (pence a kWh) | number | 24.5 | From your own bill. Rates differ by supplier, tariff, payment method and region. |
| `standing_charge_pence_per_day` | Standing charge (pence a day) | number | 60 | - |
| `uses_per_day` | Uses a day | number | "" | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Hours a day must be between 0 and 24.
- A power beyond a domestic supply is refused.
- Negative prices are refused.

## Formula and logic

Energy is power times hours; the bill is energy at the unit rate plus the standing charge for each day.

## Outputs

- `energy_kwh`
- `energy_cost`
- `standing_charge_cost`
- `total_cost`
- `cost_per_day`
- `kwh_per_year`
- `cost_per_year`
- `standing_charge_share_pct`
- `cost_per_use`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A 2 kW heater for an hour a day over a year** - {"power_watts":2000,"hours_per_day":1,"days":365,"price_pence_per_kwh":24.5,"standing_charge_pence_per_day":60,"uses_per_day":null}
- **A 60 W bulb left on all the time for a month** - {"power_watts":60,"hours_per_day":24,"days":30,"price_pence_per_kwh":24.5,"standing_charge_pence_per_day":60,"uses_per_day":null}
- **A tumble dryer, priced per cycle** - {"power_watts":2500,"hours_per_day":1.5,"days":52,"price_pence_per_kwh":26,"standing_charge_pence_per_day":55,"uses_per_day":1}
- **A standby load of 5 W, where the standing charge dwarfs the energy** - {"power_watts":5,"hours_per_day":24,"days":365,"price_pence_per_kwh":24.5,"standing_charge_pence_per_day":60,"uses_per_day":null}
- **An off-peak tariff** - {"power_watts":7000,"hours_per_day":4,"days":200,"price_pence_per_kwh":7.5,"standing_charge_pence_per_day":45,"uses_per_day":null}
- **No standing charge at all** - {"power_watts":800,"hours_per_day":3,"days":90,"price_pence_per_kwh":22,"standing_charge_pence_per_day":0,"uses_per_day":null}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

THE STANDING CHARGE IS SHOWN SEPARATELY AND AS A SHARE OF THE TOTAL, because it is paid whether or not the appliance runs. Folding it into the unit rate makes a rarely-used appliance look far more expensive than it is: on a five watt standby load it is over ninety per cent of the bill, and a comparison that hid it would be meaningless. THE RATED POWER IS A MAXIMUM, NOT AN AVERAGE. A fridge, a washing machine, an oven or anything thermostatically controlled draws its rated power only part of the time, so a plug-in energy monitor will read lower than this. The rate and standing charge are entered from your own bill rather than assumed, because they differ by supplier, tariff, payment method and region, and any figure hard-coded today would be wrong within months.

## Methodology

The oracle accumulates cost one day at a time rather than multiplying out. A standby-load case exists specifically to make the standing charge dominate, and a zero-standing-charge case pins the boundary where the total must equal the energy cost exactly.

## Related calculators

- SCI-011 BTU Calculator
- SCI-001 Ohm's Law

## SEO metadata

- Title: `Electricity Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/electricity-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
