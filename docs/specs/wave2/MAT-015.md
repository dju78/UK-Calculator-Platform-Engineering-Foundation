# MAT-015 - Factor Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-015` |
| Name | Factor Calculator |
| Category | Maths & Algebra |
| Subcategory | Number Theory |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/factor-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

List every factor of a number and classify it.

## Scope

A whole number of 1 or more, up to a working limit.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `number` | Number | number | 28 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Numbers below 1, and above the working limit, are refused.

## Formula and logic

Divisors are found up to the square root and paired, which is what makes the search fast. A number is perfect when its proper divisors sum to itself, abundant when they sum to more, deficient when less.

## Outputs

- `factors`
- `factor_count`
- `sum_of_proper_divisors`
- `largest_proper_factor`
- `is_prime`
- `is_perfect`
- `is_abundant`
- `is_deficient`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A prime** - {"number":97}
- **A perfect number** - {"number":28}
- **A power of two** - {"number":64}
- **A product of distinct primes** - {"number":210}
- **A square number** - {"number":144}
- **An abundant number** - {"number":12}
- **A large semiprime** - {"number":10403}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Same range limit as MAT-014.

## Methodology

The oracle tests EVERY candidate from 1 to n, where the engine tests only up to the square root and pairs the results. The two lists must match exactly.

## Related calculators

- MAT-014 Prime Factorisation
- MAT-016 Greatest Common Factor

## SEO metadata

- Title: `Factor Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/factor-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
