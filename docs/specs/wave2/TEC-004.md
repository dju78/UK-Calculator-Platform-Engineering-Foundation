# TEC-004 - URL Encode / Decode

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `TEC-004` |
| Name | URL Encode / Decode |
| Category | Technology & Digital |
| Subcategory | Encoding |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/url-encode-decode` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Percent-encode and decode text, for a whole URL or for a single parameter value.

## Scope

Text of up to 100,000 characters, in either direction, in either mode.

## Assumptions

- UTF-8 percent-encoding, as required by modern URL standards.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `direction` | Encode or decode? | select | "encode" | - |
| `mode` | What are you encoding? | select | "component" | These are not interchangeable: a value must escape the characters a whole URL keeps. |
| `text` | Text | text | "a=1&b=2 c" | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Malformed percent-encoding is refused with the reason, including the point that a literal percent sign must be written as %25.
- Oversized inputs are refused.

## Formula and logic

Unreserved characters pass through; everything else becomes a percent sign and two hexadecimal digits per UTF-8 byte. The two modes differ in whether the reserved structural characters are escaped.

## Outputs

- `output`
- `input_length`
- `output_length`
- `characters_changed`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **A parameter value containing an ampersand and an equals sign** - {"direction":"encode","text":"a=1&b=2 c","mode":"component"}
- **The same characters inside a whole URL, where they are structural** - {"direction":"encode","text":"https://x.co/a b?q=1&r=2","mode":"full_url"}
- **A value with a hash, which would otherwise truncate the URL** - {"direction":"encode","text":"colour#ff0000","mode":"component"}
- **Non-ASCII text, encoded as UTF-8 bytes** - {"direction":"encode","text":"café","mode":"component"}
- **A value needing no encoding at all** - {"direction":"encode","text":"already-safe_value.123","mode":"component"}
- **A path with spaces in a whole URL** - {"direction":"encode","text":"https://example.com/my file.pdf","mode":"full_url"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

TWO ENCODINGS EXIST AND THEY ARE NOT INTERCHANGEABLE. Encoding a WHOLE URL leaves the structural characters alone, because the colon, slash, question mark, hash, ampersand and equals all carry meaning there. Encoding a single PARAMETER VALUE must escape those same characters, because inside a value they are data rather than structure. GETTING THIS WRONG IS A REAL BUG, NOT A COSMETIC ONE. A value containing an ampersand, encoded with the whole-URL rules, silently splits into two parameters; a value containing a hash loses everything after it. Both failures are silent, produce a URL that looks fine, and are found in production rather than in review. That is why the mode is an explicit choice here rather than a default, and why a unit test asserts the two modes give DIFFERENT answers for the same input.

## Methodology

The oracle builds the encoding from an EXPLICIT list of unreserved characters and a hand-written hexadecimal formatter, never from encodeURIComponent, so it does not merely re-run the platform function the engine uses. Benchmark cases include the same structural characters in both modes so the difference is visible in the fixtures themselves.

## Related calculators

- TEC-003 Base64 Encode / Decode

## SEO metadata

- Title: `URL Encode / Decode | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/url-encode-decode`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
