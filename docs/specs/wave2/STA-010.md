# STA-010 - Statistical Power Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-010` |
| Name | Statistical Power Calculator |
| Category | Statistics & Data |
| Subcategory | Research |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/statistical-power-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Work out the power of a planned comparison, and the sample size a target power would need.

## Scope

A two-sample comparison of means with equal group sizes.

## Assumptions

- Equal group sizes.
- The effect size is expressed as Cohen's d.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `effect_size` | Effect size (Cohen's d) | number | 0.5 | 0.2 small, 0.5 medium, 0.8 large. |
| `sample_size_per_group` | Sample size per group | number | 100 | - |
| `alpha` | Significance level | number | 0.05 | - |
| `target_power` | Target power | number | 0.8 | 0.8 is the usual convention. |
| `two_tailed` | Two-tailed test? | select | "true" | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Each group needs at least two observations; alpha and the target power must be strictly between 0 and 1.

## Formula and logic

Power is computed from the non-centrality d times the square root of n over 2. The required sample size uses 2(z_alpha + z_beta)^2 / d^2 PLUS z_alpha^2 / 4. The plain normal approximation without that correction understates the requirement, because the test statistic is a t and not a z; with it, this reproduces the published two-sample requirements exactly - 64 per group at d = 0.5, 26 at d = 0.8 and 394 at d = 0.2, all at 5% and 80% power.

## Outputs

- `effect_size`
- `power`
- `beta`
- `required_sample_size_per_group`
- `critical_value`
- `is_adequately_powered`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Medium effect, well powered** - {"effect_size":0.5,"sample_size_per_group":100,"alpha":0.05,"target_power":0.8,"two_tailed":true}
- **Medium effect, under-powered** - {"effect_size":0.5,"sample_size_per_group":30,"alpha":0.05,"target_power":0.8,"two_tailed":true}
- **Large effect needs a much smaller sample** - {"effect_size":0.8,"sample_size_per_group":30,"alpha":0.05,"target_power":0.8,"two_tailed":true}
- **Small effect is expensive to detect** - {"effect_size":0.2,"sample_size_per_group":100,"alpha":0.05,"target_power":0.8,"two_tailed":true}
- **One-tailed test has more power for the same sample** - {"effect_size":0.5,"sample_size_per_group":100,"alpha":0.05,"target_power":0.8,"two_tailed":false}
- **Stricter alpha costs power** - {"effect_size":0.5,"sample_size_per_group":100,"alpha":0.01,"target_power":0.9,"two_tailed":true}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Under-powered studies are the commonest route to a wrong conclusion, in both directions: they miss real effects, and the effects they do find are exaggerated. An under-powered design is flagged with the sample size that would fix it, and the calculator states plainly that finding nothing is not the same as showing there is nothing to find. A statistical result on the data entered. Statistical significance is not the same as practical importance, and no test can tell you whether the data were collected in a way that supports the conclusion you want to draw.

## Methodology

Benchmarks come from an independent oracle that shares NO special-function code with the engine. Where the engine evaluates the t, chi-square and F distributions through the regularised incomplete beta and incomplete gamma functions, the oracle integrates each density numerically by Simpson's rule and obtains the normalising constant by integrating the unnormalised density over its whole support. No gamma function is used on that side at all, and infinite ranges are handled by substitution rather than truncation, so no tail is silently discarded. The normal quantiles on the oracle side come from bisection on a numerically integrated CDF, so the engine's Acklam-plus-Halley inverse is checked against quadrature rather than against another rational approximation.

## Related calculators

- STA-008 Sample Size
- STA-016 T-Test
- STA-019 A/B Test

## SEO metadata

- Title: `Statistical Power Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/statistical-power-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
