# INV-013 - Payback Period Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `INV-013` |
| Name | Payback Period Calculator |
| Category | Investing & Wealth |
| Subcategory | Returns |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/payback-period-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 5 |

## Purpose

Work out how long an investment takes to repay its initial outlay.

## Scope

An initial outlay followed by annual cash flows.

## Assumptions

- Cash flows arrive at the end of each year.
- Recovery within a year is interpolated evenly.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `initial_investment` | Initial investment (£) | number | 10000 | - |
| `annual_cashflows` | Annual cash flows (£) | text | "[3000 | - |
| `discount_rate` | Discount rate (%) | number | 0 | Used for the discounted payback period. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.


## Formula and logic

Cumulative cash flow crossing zero, with linear interpolation inside the crossing year. The discounted version divides each flow by (1+d)^t first.

## Outputs

- `payback_years`
- `discounted_payback_years`
- `total_cash_returned`
- `net_gain`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Even cash flows** - {"initial_investment":10000,"annual_cashflows":"[3000,3000,3000,3000,3000]","discount_rate":0}
- **With discounting** - {"initial_investment":10000,"annual_cashflows":"[3000,3000,3000,3000,3000]","discount_rate":8}
- **Never pays back** - {"initial_investment":50000,"annual_cashflows":"[5000,5000,5000]","discount_rate":0}
- **Fast payback** - {"initial_investment":5000,"annual_cashflows":"[4000,4000]","discount_rate":0}
- **Uneven flows** - {"initial_investment":20000,"annual_cashflows":"[2000,5000,8000,10000]","discount_rate":5}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Payback ignores everything after the outlay is recovered, so it measures liquidity rather than profitability. Cash flows that never recover the outlay report no payback rather than a misleading number.

## Methodology

Independently derived in the oracle by explicit simulation or summation rather than the closed forms the engine uses.

## Related calculators

- INV-011 IRR Calculator
- INV-008 ROI Calculator

## SEO metadata

- Title: `Payback Period Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/payback-period-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (5)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
