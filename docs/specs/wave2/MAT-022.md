# MAT-022 - Binary Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-022` |
| Name | Binary Calculator |
| Category | Maths & Algebra |
| Subcategory | Number Systems |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/binary-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Convert and compute in binary, with the other common bases shown alongside.

## Scope

Conversion and integer arithmetic in base 2.

## Assumptions

- Whole numbers only.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `operation` | Operation | select | "convert" | - |
| `value_a` | Binary value | text | "1011" | - |
| `value_b` | Second binary value | text | "" | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Invalid digits are named, along with which digits the base allows.
- Values beyond exact integer representation are refused.
- Division by zero is refused.

## Formula and logic

Values are parsed from their digits and rebuilt by repeated division.

## Outputs

- `decimal_result`
- `binary`
- `octal`
- `decimal`
- `hexadecimal`
- `bit_length`
- `twos_complement_8_bit`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Convert binary to decimal** - {"operation":"convert","value_a":"1011","value_b":""}
- **Binary addition** - {"operation":"add","value_a":"1011","value_b":"1101"}
- **Binary subtraction** - {"operation":"subtract","value_a":"11000","value_b":"1011"}
- **Binary multiplication** - {"operation":"multiply","value_a":"101","value_b":"110"}
- **Binary integer division** - {"operation":"divide","value_a":"100110","value_b":"101"}
- **A byte at its maximum** - {"operation":"convert","value_a":"11111111","value_b":""}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

Division is INTEGER division, so any fractional part is discarded, as it is in most low-level arithmetic. Two's complement is shown only where the value fits in eight bits, because the representation is meaningless without a fixed width.

## Methodology

The oracle parses and rebuilds digits by EXPLICIT ARITHMETIC, never with parseInt or toString, so the engine's use of the built-ins is independently checked.

## Related calculators

- MAT-023 Hex Calculator
- TEC-001 IP Subnet

## SEO metadata

- Title: `Binary Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/binary-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
