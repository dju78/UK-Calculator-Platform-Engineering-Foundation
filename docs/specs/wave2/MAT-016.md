# MAT-016 - Greatest Common Factor Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `MAT-016` |
| Name | Greatest Common Factor Calculator |
| Category | Maths & Algebra |
| Subcategory | Number Theory |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/greatest-common-factor-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 6 |

## Purpose

Find the greatest common factor of two or more numbers, with the lowest common multiple alongside.

## Scope

Two or more non-zero whole numbers.

## Assumptions

_Not yet authored._

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `numbers` | Numbers | text | "[48 | At least two whole numbers. |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- At least two numbers; zero is refused, because it has no greatest common factor with another number.

## Formula and logic

The Euclidean algorithm, applied pairwise. The lowest common multiple is built as a divided by the common factor, then multiplied by b - dividing FIRST so the intermediate value stays small.

## Outputs

- `greatest_common_factor`
- `least_common_multiple`
- `are_coprime`
- `product_of_all`
- `simplified_ratio`
- `gcf_times_lcm`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Two numbers with a common factor** - {"numbers":"[48,18]"}
- **Coprime numbers** - {"numbers":"[35,64]"}
- **One divides the other** - {"numbers":"[7,49]"}
- **Three numbers** - {"numbers":"[12,18,24]"}
- **Four numbers** - {"numbers":"[8,12,20,28]"}
- **Identical numbers** - {"numbers":"[15,15]"}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

The identity that the greatest common factor times the lowest common multiple equals the product holds for exactly TWO numbers. It does not extend to three or more, so it is reported only for a pair rather than presented as a general law.

## Methodology

The oracle uses the original Euclidean method of REPEATED SUBTRACTION rather than the modulo form the engine uses.

## Related calculators

- MAT-017 Least Common Multiple
- MAT-014 Prime Factorisation

## SEO metadata

- Title: `Greatest Common Factor Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/greatest-common-factor-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (6)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
