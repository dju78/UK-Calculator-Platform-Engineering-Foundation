# BUS-011 - Pricing Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `BUS-011` |
| Name | Pricing Calculator |
| Category | Business & Commercial |
| Subcategory | Pricing |
| Launch wave | Wave 2 |
| Jurisdiction | UK/General |
| Regulatory risk | low |
| Rules-sensitive | Yes |
| Canonical route | `/calculators/pricing-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

Price a product from a target margin, and show what a discount does to that margin.

## Scope

One product: unit cost, target margin, VAT registration, an optional discount and fixed costs to cover.

## Assumptions

- The margin entered is a margin, not a markup.
- VAT is the standard rate, read from the versioned ruleset.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `unit_cost` | Unit cost (£) | number | 70 | - |
| `target_margin` | Target margin (%) | number | 30 | Margin, not markup. Enter 30 for 30%. |
| `vat_registered` | VAT registered? | select | "true" | - |
| `discount` | Discount you plan to offer (%) | number | 0 | - |
| `fixed_costs` | Fixed costs to cover (£) | number | 0 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- A margin of 100% or more is rejected, because the price would have to be infinite.
- A negative margin, or a discount outside 0% to 100%, is rejected.
- Where the discount takes the price to or below cost, the calculator warns that every sale loses money.

## Formula and logic

Price is `cost / (1 - margin)`. Multiplying cost by one plus the margin would be a MARKUP and would miss the target badly at high percentages. Break-even volume is fixed costs divided by the contribution per unit after any discount.

## Outputs

- `price_excluding_vat`
- `vat_amount`
- `price_including_vat`
- `profit_per_unit`
- `markup_on_cost`
- `price_after_discount_excluding_vat`
- `margin_after_discount`
- `break_even_units`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **30% target margin, VAT registered** - {"unit_cost":70,"target_margin":30,"vat_registered":true,"discount":0,"fixed_costs":0}
- **A 20% discount on a 30% margin leaves 12.5%, not 10%** - {"unit_cost":70,"target_margin":30,"vat_registered":true,"discount":20,"fixed_costs":0}
- **Not VAT registered** - {"unit_cost":40,"target_margin":50,"vat_registered":false,"discount":0,"fixed_costs":0}
- **High margin product** - {"unit_cost":5,"target_margin":80,"vat_registered":true,"discount":0,"fixed_costs":0}
- **Discount that wipes out the margin entirely** - {"unit_cost":70,"target_margin":20,"vat_registered":true,"discount":25,"fixed_costs":0}
- **With fixed costs to cover** - {"unit_cost":12,"target_margin":40,"vat_registered":true,"discount":0,"fixed_costs":30000}
- **Zero margin sells at cost** - {"unit_cost":100,"target_margin":0,"vat_registered":true,"discount":0,"fixed_costs":0}

## Rules and source dependencies

The VAT standard rate is read from the versioned UK Rules Engine rather than being typed into a form, so a rate change is a ruleset change.

## Product boundary

A discount comes straight off profit, not off the margin percentage, which is why a 20% discount on a 30% margin leaves 12.5% and not 10%. Businesses routinely discount themselves into a loss by subtracting the discount from the margin, so a matched benchmark pair proves the correct behaviour.

## Methodology

Benchmarks were derived independently, with the VAT standard rate re-typed from the primary source rather than read from the ruleset, so agreement corroborates the ruleset value as well as the arithmetic.

## Related calculators

- BUS-002 Markup
- BUS-001 Margin Calculator
- BUS-006 Break-Even

## SEO metadata

- Title: `Pricing Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/pricing-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
