# STA-007 - P-Value Calculator

> Generated from the registry, field definitions and canonical benchmarks by
> `scripts/generate_wave2_specs.mjs`. Do not hand-edit: change the source
> artefacts and regenerate, so the specification cannot drift from the code.

## Identity

| Field | Value |
|---|---|
| ID | `STA-007` |
| Name | P-Value Calculator |
| Category | Statistics & Data |
| Subcategory | Inference |
| Launch wave | Wave 2 |
| Jurisdiction | General |
| Regulatory risk | low |
| Rules-sensitive | No |
| Canonical route | `/calculators/p-value-calculator` |
| Status | verified |
| Implementation status | implemented |
| Benchmark cases | 7 |

## Purpose

Convert a test statistic into a p-value under the normal, t, chi-square or F distribution.

## Scope

One statistic, its distribution, its degrees of freedom and the tail of interest.

## Assumptions

- The statistic was computed correctly for the distribution named.

## Inputs

| Name | Label | Type | Default | Units / notes |
|---|---|---|---|---|
| `test_statistic` | Test statistic | number | 1.96 | - |
| `distribution` | Distribution | select | "z" | - |
| `tail` | Tail | select | "two" | Chi-square and F tests are right-tailed only. |
| `degrees_of_freedom` | Degrees of freedom | number | 0 | - |
| `degrees_of_freedom_2` | Denominator degrees of freedom | number | 0 | - |

Percentages are entered as human percentages (5 means 5%) and normalised once
inside the engine.

## Validation

Enforced centrally at the engine boundary and by the shared validation module:

- Any non-finite input is rejected with a readable message.
- Any non-finite output is rejected rather than returned, so `NaN` and
  `Infinity` can never reach a user.
- Terms are bounded (maximum 150 years) and monetary amounts capped, so no
  input can spin an amortisation loop indefinitely.
- Degrees of freedom must be greater than zero for t, chi-square and F.
- A left-tailed or two-tailed chi-square or F test is REFUSED, because those statistics are a sum of squares and a ratio of variances respectively and cannot be negative. Answering such a request would dignify a category error.

## Formula and logic

The p-value is the cumulative distribution function for a left tail, its complement for a right tail, and twice the smaller of the two for a two-tailed test.

## Outputs

- `p_value`
- `significant_at_5_percent`
- `significant_at_1_percent`

Formatting is decided by the central registry in
`apps/web/src/components/calculators/outputFormats.ts`, per calculator and per
key, so ratios render as percentages and money as `£1,234.56`.

## Edge cases covered by benchmarks

- **Two-tailed z test** - {"test_statistic":1.96,"distribution":"z","tail":"two"}
- **Right-tailed z test** - {"test_statistic":2.33,"distribution":"z","tail":"right"}
- **Left-tailed z test** - {"test_statistic":-1.645,"distribution":"z","tail":"left"}
- **Two-tailed t test with 10 degrees of freedom** - {"test_statistic":2.228,"distribution":"t","tail":"two","degrees_of_freedom":10}
- **Right-tailed t test with 25 degrees of freedom** - {"test_statistic":1.708,"distribution":"t","tail":"right","degrees_of_freedom":25}
- **Chi-square with 3 degrees of freedom** - {"test_statistic":7.815,"distribution":"chi_square","tail":"right","degrees_of_freedom":3}
- **F test with 3 and 12 degrees of freedom** - {"test_statistic":3.49,"distribution":"f","tail":"right","degrees_of_freedom":3,"degrees_of_freedom_2":12}

## Rules and source dependencies

Not rules-sensitive. No statutory values are used.

## Product boundary

A p-value is the probability of a result at least this extreme IF the null hypothesis were true. It is not the probability that the null hypothesis is true, and it says nothing about the size of an effect. A statistical result on the data entered. Statistical significance is not the same as practical importance, and no test can tell you whether the data were collected in a way that supports the conclusion you want to draw.

## Methodology

Benchmarks come from an independent oracle that shares NO special-function code with the engine. Where the engine evaluates the t, chi-square and F distributions through the regularised incomplete beta and incomplete gamma functions, the oracle integrates each density numerically by Simpson's rule and obtains the normalising constant by integrating the unnormalised density over its whole support. No gamma function is used on that side at all, and infinite ranges are handled by substitution rather than truncation, so no tail is silently discarded. Benchmarks sit on published critical points - 1.96, 2.228 at 10 degrees of freedom, 7.815 at 3 - so each must return a p-value of 0.05.

## Related calculators

- STA-016 T-Test
- STA-017 Chi-Square
- STA-018 ANOVA

## SEO metadata

- Title: `P-Value Calculator | UK Calculator Platform`
- Description: generated per calculator by `apps/web/src/lib/site.ts`
- Canonical: `/calculators/p-value-calculator`
- Structured data: `WebApplication`

## Definition of Done

- [x] At least five independently derived benchmark cases (7)
- [x] UI field definitions registered
- [x] Engine handler implemented
- [x] Registry status verified
- [x] Narrative sections authored
